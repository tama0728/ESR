name: ESR
channels:
  - defaults
dependencies:
  - python=3.10
  - pip
  - pip:
    # === Core image processing ===
    - opencv-python==4.8.1.78
    - opencv-contrib-python==4.11.0.86
    - numpy==1.26.4
    - Pillow==10.4.0
    - scikit-image==0.25.2
    - imageio==2.37.3

    # === Face detection ===
    - mediapipe==0.10.9

    # === FGResQ dependencies ===
    - timm==1.0.17
    - transformers==4.36.1
    - huggingface-hub>=0.33.0
    - safetensors==0.5.3

    # === GFPGAN / Real-ESRGAN ===
    - gfpgan==1.3.8
    - basicsr==1.4.2
    - facexlib==0.3.0
    - realesrgan==0.3.0

    # === Metric evaluation ===
    - pyiqa>=0.1.15
    - lpips==0.1.4
    - pandas==2.0.3
    - openpyxl==3.1.5

    # === Utilities ===
    - scipy==1.15.3
    - matplotlib==3.7.5
    - tqdm==4.67.1
    - pyyaml==6.0.2
    - requests>=2.32.0
    - lmdb==2.1.1
    - einops==0.8.1
    - addict==2.4.0
